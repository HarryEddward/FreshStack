name: Apply Docker Swarm Secrets to VPS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    if: true
    runs-on: ubuntu-latest
    steps:
      - name: Apply secrets to Swarm
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            set -e

            echo "📥 Actualizando secretos en Docker Swarm..."

            # Mapa de stacks a sus docker-compose.yml
            declare -A stacks_compose=(
              # [traefik]="/home/ubuntu/FreshStack/traefik/docker-compose.yml"
              # [postgresql]="/home/ubuntu/FreshStack/postgresql/docker-compose.yml"
            )

            # Eliminar completamente todos los stacks
            for stack in "${!stacks_compose[@]}"; do
              echo "🛑 Eliminando stack $stack..."
              sudo docker stack rm "$stack"

              # Esperar a que desaparezca completamente
              while sudo docker stack ls --format '{{.Name}}' | grep -q "^$stack$"; do
                echo "⏳ Esperando a que $stack desaparezca..."
                sleep 3
              done
            done

            echo "🔐 Actualizando secretos..."

            # Lista de secretos que quieres desplegar (nombre en GitHub -> nombre en Docker Swarm)
            declare -A secrets=(
              [SSL_CERT]=ssl_cert
              [SSL_KEY]=ssl_key
              [FRESH_SESSION_SECRET]=fresh_session_secret
              [JENKINS_SECRET_KEY]=jenkins_secret_key
              [KEYCLOAK_CLIENT_BACKEND_SERVER_SECRET]=keycloak_client_backend_server_secret
              [POSTGRESQL_PASSWORD]=postgresql_password
              [POSTGRESQL_USERNAME]=postgresql_username
              [SEAWEEDFS_S3_ACCESS_KEY]=seaweedfs_s3_access_key
              [SEAWEEDFS_S3_SECRET_KEY]=seaweedfs_s3_secret_key
              [STRIPE_PUBLISHABLE_KEY]=stripe_publishable_key
              [STRIPE_SECRET_KEY]=stripe_secret_key
            )

            for gh_secret in "${!secrets[@]}"; do
              swarm_secret="${secrets[$gh_secret]}"
              echo "🔐 Procesando secreto: $swarm_secret"

              # Eliminar el secreto existente
              sudo docker secret rm "$swarm_secret" || true

              # Esperar a que se borre completamente
              retries=10
              while sudo docker secret ls --format '{{.Name}}' | grep -q "^$swarm_secret$"; do
                echo "⏳ Esperando a que se elimine $swarm_secret..."
                sleep 2
                retries=$((retries-1))
                if [ $retries -le 0 ]; then
                  echo "❌ Timeout: el secreto $swarm_secret no se pudo eliminar"
                  exit 1
                fi
              done

              # Crear el secreto nuevo con el valor desde GitHub Actions
              secret_value="${!gh_secret}"
              echo "$secret_value" | sudo docker secret create "$swarm_secret" -
              echo "✅ Secreto actualizado: $swarm_secret"
            done

            echo "🚀 Volviendo a desplegar todos los stacks..."
            for stack in "${!stacks_compose[@]}"; do
              echo "🚀 Desplegando stack $stack..."
              sudo docker stack deploy -c "${stacks_compose[$stack]}" "$stack"
            done

            echo "✅ Todos los stacks actualizados y levantados"
